# Server Configuration
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# CORS Configuration
# Use * to allow all origins, or specify comma-separated domains
# Examples:
#   CORS_ORIGIN=*
#   CORS_ORIGIN=https://yourdomain.com
#   CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=*

# Sentry Error Tracking (optional)
# Leave empty to disable Sentry integration
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
# Sentry trace sampling rate (0.0 to 1.0, default: 1.0 for full sampling)
SENTRY_TRACES_SAMPLE_RATE=1.0

# Recipe Clipper ML Classifier (optional)
# Leave empty to disable ML classification (will use CSS selectors only)
# This endpoint is used for ingredient/instruction classification based on https://github.com/julianpoy/ingredient-instruction-classifier
INGREDIENT_INSTRUCTION_CLASSIFIER_URL=

# Request Limits
# Maximum request body size in bytes
MAX_BODY_SIZE=10485760
